# ================================
# Variables de Entorno - Producción
# Al Chile FB - Cloud Run Deployment
# ================================

# ================================
# FRONTEND (Next.js)
# ================================

# Firebase Client SDK (públicas)
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key-here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=studio-9824031244-700aa.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=studio-9824031244-700aa
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=studio-9824031244-700aa.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Google Maps API Key (pública)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# FCM VAPID Key (pública)
NEXT_PUBLIC_FCM_VAPID_KEY=your-fcm-vapid-key

# Backend URL (se configura después del deploy del backend)
BACKEND_URL=https://alchile-backend-xxxxx.run.app

# ================================
# BACKEND (Express)
# ================================

# Node Environment
NODE_ENV=production

# Port (Cloud Run lo asigna automáticamente)
PORT=8080

# Firebase Admin SDK (Secret Manager)
# Nota: No se pasa como variable, se monta como archivo desde Secret Manager
# GOOGLE_APPLICATION_CREDENTIALS=/secrets/firebase-admin-key

# Twilio (Secret Manager)
# TWILIO_ACCOUNT_SID=your-account-sid
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# FCM Configuration
FCM_MAX_TOKENS_PER_USER=10
FCM_TOKEN_CLEANUP_DAYS=90

# Frontend URL (para CORS)
FRONTEND_URL=https://alchile-frontend-xxxxx.run.app

# ================================
# GOOGLE CLOUD PROJECT
# ================================

PROJECT_ID=studio-9824031244-700aa
REGION=us-central1

# ================================
# NOTAS IMPORTANTES
# ================================

# 1. Variables públicas (NEXT_PUBLIC_*):
#    - Se incluyen en el build del frontend
#    - Se pasan como --build-arg en Cloud Build
#
# 2. Variables privadas del backend:
#    - Se guardan en Secret Manager
#    - Se montan en Cloud Run con --set-secrets
#
# 3. Firebase Admin SDK:
#    - Descargar JSON desde Firebase Console
#    - Subir a Secret Manager como 'firebase-admin-key'
#    - Cloud Run lo monta automáticamente en /secrets/
#
# 4. URLs de servicios:
#    - Se obtienen después del deploy
#    - Backend se despliega primero
#    - Frontend requiere BACKEND_URL del deploy anterior
